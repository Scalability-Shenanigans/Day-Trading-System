# DayTrading Inc Frontend

Frontend of the DayTrading application that is written in React.

# Usage

Run `npm install` to install the packages

Run `npm start` to start the app on localhost port 3000 (`http://localhost:3000/`)
